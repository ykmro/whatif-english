<% content_for :head do %>
  <%= javascript_include_tag("pages/top") %>
<% end %>

<main class="max-w-3xl mx-auto px-4 sm:px-6 py-16 text-center">
  <p class="text-xs font-bold tracking-widest text-primary uppercase mb-4">COMPLETE</p>
  <h1 class="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-6">お疲れ様でした！</h1>

  <p class="text-gray-600 dark:text-gray-300 leading-loose mb-12 text-sm sm:text-base">
    適切な回答を選ぶことができましたか？<br>
    定期的に英語に触れることで長期記憶に残りやすくなります。<br>
    ぜひ習慣化してみてくださいね！
  </p>

  <h2 class="text-sm font-bold text-gray-400 dark:text-gray-500 tracking-widest uppercase mb-5">あなたが選んだ選択肢</h2>
  <div class="space-y-3 mb-12 text-left max-w-lg mx-auto">
    <% @choices.each do |choice| %>
      <div class="border border-gray-200 dark:border-gray-700 rounded-xl px-5 py-4 bg-gray-50 dark:bg-gray-800 transition-colors duration-200">
        <span class="text-gray-700 dark:text-gray-300 text-sm sm:text-base"><%= choice.body %></span>
      </div>
    <% end %>
  </div>

  <%= link_to "TOPへ戻る", root_path,
        class: "inline-flex items-center justify-center bg-primary text-white font-bold rounded-full px-10 py-3 hover:opacity-80 transition-opacity duration-300" %>
</main>
